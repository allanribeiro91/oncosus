# Copy to .env in project root (oncosus-novo/) or backend/rag/
# API_PORT=8000
# VECTOR_DB_PATH=data/vectorstore
# Modelo Ollama (nome do `ollama create`, ex.: oncosus-llm)
# ONCOSUS_OLLAMA_MODEL=oncosus-llm
